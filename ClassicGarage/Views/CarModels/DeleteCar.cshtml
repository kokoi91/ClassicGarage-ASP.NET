@model ClassicGarage.Models.CarModels

@{
    ViewBag.Title = "Delete";
}


<div>

    Czy chcesz usunąć  <b>@Html.DisplayFor(model => model.Name)</b>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            @Html.Hidden("id", Model.Id)
            <input type="submit" value="Usuń" formaction=@Url.Action("DeleteConfirmed","CarModels") />
            @Html.ActionLink("NIE", "Index", "CarModels", new object { }, new { @class = "btn btn-default" })
        </div>
    }
</div>
